# Day 02:


### Searched for
- standart way to format code: found (built in cargo formatter `rustfmt`).
- standart lintting tool: found (built in cargo linting `clippy`).
- My first impresion was `shadowing` is bad and dangrous 😂. found a debate on reddit talking about that.
- what happens to the original variable after shodowing? does it removed from the memory?
- how to check a variable type at runtime (`type_name_of_val()` from `std::any`).
- string concatination (`s1 + &s2`, `fromat!()`, `.push_str()` for mut string).
- how to control the step value in the `for` loop, found (`setp_by(n)` and `rev()`).

### Articles
- [Rust Shadowing](https://bhh32.com/tutorials/rust_shadowing_tutorial).

### Courses & Challenges
- Completed `Chapter 2 & 3` of [tour-of-rust](https://tourofrust.com/TOC_en.html).
- Solved a leetcode problem `Valid Parentheses` with rust [LeetCode](https://leetcode.com/submissions/detail/1979668816/) - or see - [The code on github](https://github.com/BahiHabash/LeetCode/tree/ce39029096ba4ac287df98501964b2736b3c969b/0020-valid-parentheses)